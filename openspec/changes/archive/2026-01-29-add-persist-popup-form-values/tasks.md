## 1. 儲存層
- [x] 1.1 在 popup 中實作讀取/寫入 `chrome.storage.local` 的輔助函數（例如單一 key 存整個表單狀態：cinema, movie, date, time, ticketTypeKeyword, quantity）
- [x] 1.2 確認 manifest 無需額外權限（MV3 下 storage 預設可用）；若有需要則加入 `storage` 權限

## 2. 寫入時機
- [x] 2.1 在影城、片名、日期、時間、票數的 `change` 事件中寫入當前表單狀態
- [x] 2.2 在票種關鍵詞輸入框的 `input` 或 `change` 事件中寫入當前值（可防抖以減少寫入次數）

## 3. 還原時機與條件
- [x] 3.1 popup 初始化時從 storage 讀取；在 `loadCinemas()` 完成並填入影城選單後，若儲存的影城值存在於當前選項則設為該值並觸發後續級聯（loadMovies）
- [x] 3.2 在 `loadMovies()` 完成並填入片名選單後，若儲存的片名值存在於當前選項則設為該值並觸發 loadDates
- [x] 3.3 在 `loadDates()` 完成並填入日期選單後，若儲存的日期值存在於當前選項則設為該值並觸發 loadSessions
- [x] 3.4 在 `loadSessions()` 完成並填入時間選單後，若儲存的時間值存在於當前選項則設為該值
- [x] 3.5 還原票種關鍵詞與票數（無需檢查選項是否存在）；票數若為合法值（1–8）才還原

## 4. 驗證
- [ ] 4.1 手動測試：選擇影城/片名/日期/時間/票種關鍵詞/票數後關閉 popup 再開啟，確認在 API 資料仍存在時選項被還原
- [ ] 4.2 手動測試：儲存某影城後，API 回傳的影城列表不再包含該值時，不還原該欄位（或還原為空/預設）
